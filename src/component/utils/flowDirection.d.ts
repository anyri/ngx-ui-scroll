import { Direction } from '../interfaces/direction';
import { Viewport } from '../classes/viewport';
import { Buffer } from '../classes/buffer';
export declare const calculateFlowDirection: (viewport: Viewport, buffer: Buffer) => Direction;
